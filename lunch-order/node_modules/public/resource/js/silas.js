$(document).ready(function() {
  $("code").each(function() {
    if (this.innerText.match(/^\$/)) return;
    if (this.innerText.match(/^\[/)) return;
    if (this.innerText.split("\n").length < 5) return;
    $(this).addClass("prettyprint");
  });
  prettyPrint();
});
